# Phase 2A MVP Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add the next MVP slice: 20 merge levels, skin list UI, unlock feedback, daily reward, and audio hook calls while preserving the current playable merge loop.

**Architecture:** Keep rules and data in `assets/scripts/data` and `assets/scripts/gameplay`; keep player progression state in `PlayerData`/`GameManager`; keep runtime demo UI composition in `SceneBootstrap` and view behavior in `MainView`/`SkinView`. Tests are updated before implementation to lock the expected behavior.

**Tech Stack:** Cocos Creator 3.8.x TypeScript, Node test runner, `tsx` for TypeScript logic tests, Koa backend unchanged.

---

### Task 1: Extend Progression Data

**Files:**
- Modify: `assets/scripts/data/ItemConfig.ts`
- Modify: `assets/scripts/data/SkinConfig.ts`
- Modify: `tests/client-logic.test.ts`

- [ ] Add failing tests that expect 20 item levels and skin unlocks through level 20.
- [ ] Extend item configs to 20 levels and keep `nextId = 0` only for level 20.
- [ ] Extend skin configs with late-game unlocks.
- [ ] Run `npx.cmd --yes --package tsx tsx --test tests\client-logic.test.ts`.

### Task 2: Add Daily Reward State

**Files:**
- Modify: `assets/scripts/data/PlayerData.ts`
- Modify: `assets/scripts/core/GameManager.ts`
- Modify: `tests/client-logic.test.ts`

- [ ] Add failing tests for default daily reward fields and one-claim-per-day behavior.
- [ ] Add `lastDailyRewardDate` and `dailyRewardClaimedCount` to player data.
- [ ] Add `GameManager.claimDailyReward(todayKey?)` with coin reward and save.
- [ ] Run client logic tests.

### Task 3: Add Skin UI and Audio Hooks

**Files:**
- Modify: `assets/scripts/ui/SceneBootstrap.ts`
- Modify: `assets/scripts/ui/MainView.ts`
- Modify: `assets/scripts/ui/SkinView.ts`
- Modify: `assets/scripts/core/AudioManager.ts`
- Modify: `tests/client-scaffold.test.js`

- [ ] Add failing tests requiring skin button/panel, unlock feedback, daily reward button, and audio calls.
- [ ] Add runtime skin button and daily reward button in `SceneBootstrap`.
- [ ] Implement `SkinView` label rendering for unlocked/locked skins.
- [ ] Wire `MainView` to open/close skin panel, claim daily reward, and call click/merge/fail/unlock audio hooks.
- [ ] Run scaffold tests and TypeScript script check.

### Task 4: Full Verification

**Files:**
- Test only.

- [ ] Run `node --test tests\server.test.js tests\client-scaffold.test.js`.
- [ ] Run `npx.cmd --yes --package tsx tsx --test tests\client-logic.test.ts`.
- [ ] Run filtered `tsc --noEmit` script check.
